<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.9caculator" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.9caculator.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.9caculator.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.9caculator.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="btn_number">
        <item name="android:layout_width">30pt</item>
        <item name="android:layout_height">40pt</item>
        <item name="android:background">@color/font_2</item>
        <item name="android:textSize">30dp</item>
        <item name="android:fontFamily">@font/mikhak</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_weight">1</item>
    </style>
    <style name="zero_btn" parent="btn_number">
        <item name="android:layout_weight">2</item>
        <item name="android:layout_width">60pt</item>
    </style>
    <style name="operation_btn" parent="btn_number">
        <item name="android:background">@color/operation_btn</item>
    </style>
</resources>